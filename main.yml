- name: main
  hosts: all
  tasks:
#     - name: "print vars"
#       debug: 
#         var=hostvars[inventory_hostname]

    - name: "task starting"
      shell: "date && echo start"
  
    - name: "task sleep"
      shell: "sleep 30"
      
    - name: "task finished"
      shell: "date && echo end"
    
    - name: "debug name"
      debug: msg="hello {{name}} bye bye"
